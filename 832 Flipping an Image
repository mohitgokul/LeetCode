/**
 * @param {number[][]} A
 * @return {number[][]}
 */
var flipAndInvertImage = function(A) {
    for(var i = 0; i< A.length; i++){
        A[i].reverse();
        for ( var j = 0; j< A[0].length; j++)
            A[i][j] = A[i][j] ? 0 : 1;
    }
    return A;
};
